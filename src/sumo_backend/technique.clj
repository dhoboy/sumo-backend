(ns sumo-backend.technique)

;; have all these written as json
;; technique category -> technique -> technique_ja
;; load that file in when starting the project....
;; when encountering something that isn't in the file
;; write it to the file so its there from now on

;; TODO - namespace for writing to files...
(def technique_ja->en
  {:techniques "will go here soon"})
